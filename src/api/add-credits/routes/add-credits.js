'use strict';

module.exports = {
  "routes": [
    {
      "method": "POST",
      "path": "/add-credits/add-credits",
      "handler": "add-credits.addCredits",
      "config": {
      }
    },
  ]
}