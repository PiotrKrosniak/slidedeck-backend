'use strict';

module.exports = {
  "routes": [
    {
      "method": "GET",
      "path": "/stripe/get-credit-and-subscription-plans",
      "handler": "stripe.getCreditAndSubscriptionPlans",
    },
  ]
}